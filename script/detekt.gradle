apply plugin: 'io.gitlab.arturbosch.detekt'

detekt {
    autoCorrect true
    config = files("$rootDir/detekt.yml")
    source = files(
            "src/main/kotlin",
            "src/test/kotlin",
    )
    parallel true
    buildUponDefaultConfig true
}
